import { defineConfig } from 'astro/config';

export default defineConfig({
  // La landing page se sirve desde la raíz
  site: 'https://919it.org',
  // Carpeta de output para producción
  outDir: './dist',
});
